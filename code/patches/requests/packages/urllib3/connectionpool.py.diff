--- requests/packages/urllib3/connectionpool.py.orig	2015-06-25 11:17:16.630252555 +0000
+++ requests/packages/urllib3/connectionpool.py	2015-06-25 11:21:17.321847116 +0000
@@ -236,7 +236,11 @@
         # If this is a persistent connection, check if it got disconnected
         if conn and is_connection_dropped(conn):
             log.info("Resetting dropped connection: %s" % self.host)
-            conn.close()
+            try:
+                conn.close()
+            except Exception as e:
+                log.info("Closing connection failed: %s, force new connection" % str(e))
+                conn = None
             if getattr(conn, 'auto_open', 1) == 0:
                 # This is a proxied connection that has been mutated by
                 # httplib._tunnel() and cannot be reused (since it would
@@ -308,12 +312,6 @@
         if hasattr(err, 'errno') and err.errno in _blocking_errnos:
             raise ReadTimeoutError(self, url, "Read timed out. (read timeout=%s)" % timeout_value)
 
-        # Catch possible read timeouts thrown as SSL errors. If not the
-        # case, rethrow the original. We need to do this because of:
-        # http://bugs.python.org/issue10272
-        if 'timed out' in str(err) or 'did not complete (read)' in str(err):  # Python 2.6
-            raise ReadTimeoutError(self, url, "Read timed out. (read timeout=%s)" % timeout_value)
-
     def _make_request(self, conn, method, url, timeout=_Default,
                       **httplib_request_kw):
         """
